{% extends 'SiteMainBundle:Backend:layout.html.twig' %}

{% block content -%}
    <h1>{{ 'backend.class_room.h1_create'|trans({}, 'menu') }}</h1>

    {{ form(form, { 'style': 'horizontal', attr: {novalidate: 'novalidate'} }) }}

    <ul class="record_actions nav nav-pills">
        <li>
            <a href="{{ path('backend_class_room_index') }}">
                {{ 'backend.class_room.back_to_list'|trans({}, 'menu') }}
            </a>
        </li>
    </ul>
{% endblock %}
